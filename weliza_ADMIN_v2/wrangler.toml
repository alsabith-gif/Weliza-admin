name = "weliza-admin"
compatibility_date = "2024-01-01"
pages_build_output_dir = "."

[vars]
# Set ADMIN_PASSWORD as a secret in Cloudflare dashboard, not here
# Run: wrangler pages secret put ADMIN_PASSWORD
